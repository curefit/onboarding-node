/**
 * Data Types
 * 1. Define Array of book names. Append size of array to array itself (hint: array has .push() method)
 * 2. Print person's name, gender, weight and age using same variable
 * 3. Create person json object. Identify if person is minor using age field. If yes, append isMinor: true field.
 *
 * Functions
 * 1. Compute nth fibonacci number.
 * 2. Compute square of a number. Must define and use square() and multiple() method.
 *
 *
 * A json problem
 * */